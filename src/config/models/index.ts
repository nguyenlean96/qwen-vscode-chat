import { SUPPORTED_MODELS as QWEN_MODELS } from "./qwen";
import { SUPPORTED_MODELS as MINIMAX_MODELS } from "./minimax";
import { SUPPORTED_MODELS as DEEPSEEK_MODELS } from "./deepseek";
import { SUPPORTED_MODELS as KIMI_MODELS } from "./moonshot";
import { SUPPORTED_MODELS as GLM_MODELS } from "./zhipu";

export {
  QWEN_MODELS,
  MINIMAX_MODELS,
  DEEPSEEK_MODELS,
  KIMI_MODELS,
  GLM_MODELS,
};